package com.orion.app.data

/**
 * Enum representing user roles in Live Location feature
 */
enum class UserRole {
    /** Visually impaired user whose location is shared */
    CHILD,
    /** Parent/caregiver who monitors the location */
    PARENT
}
